interface Pet {
    name: String
    age: Int
}

type Person {
    name: String
    age: Int
    pet: [Pet]
    knows: [Person!]!
}

type Dog implements Pet {
    name: String
    age: Int
}

type Cat implements Pet {
    name: String
    age: Int
}